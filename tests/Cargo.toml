[package]
name = "tests"
version = "0.1.0"
authors = ["Andrew Jones <ascjones@gmail.com>"]

[dependencies]
jsonrpc-core = { version = "9.0", path = "../core" }
jsonrpc-derive = { path = "../derive" }
jsonrpc-macros = { path = "../macros" }