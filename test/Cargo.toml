[package]
name = "jsonrpc-test"
description = "Simple test framework for JSON-RPC."
version = "10.1.0"
authors = ["Tomasz Drwięga <tomasz@parity.io>"]
license = "MIT"
homepage = "https://github.com/paritytech/jsonrpc"
repository = "https://github.com/paritytech/jsonrpc"
documentation = "https://docs.rs/jsonrpc-test/"
edition = "2018"

[dependencies]
jsonrpc-core = { path = "../core", version = "10.0" }
jsonrpc-pubsub = { path = "../pubsub", version = "10.0" }
serde = "1.0"
serde_json = "1.0"

[dev-dependencies]
jsonrpc-derive = { path = "../derive", version = "10.0" }

